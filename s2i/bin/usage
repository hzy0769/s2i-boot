#!/bin/bash -e
cat <<EOF
This is the s2i-boot S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build https://github.com/linzhaoming/springboot-helloworld linzhaoming/s2i-boot example-s2i-boot

You can then run the resulting image via:
docker run <application image>
EOF
